package exercicios

/**
 * Ponto de entrada principal do programa.
 *
 * Este programa simples imprime a saudação "Hello World!" no console.
 * Ele demonstra a estrutura básica de um programa em Kotlin, incluindo:
 * - A declaração da função `main`, que é o ponto de partida da execução.
 * - A declaração de uma variável `SaudacaoMundo` para armazenar a string da saudação.
 * - O uso da função `println` para exibir a saudação na saída padrão.
 */

fun main() {
    var SaudacaoMundo = "Hello World!"
    println(SaudacaoMundo)
}